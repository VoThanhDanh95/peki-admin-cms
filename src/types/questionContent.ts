export type SimpleQuestionContent = {
    id: string
    content: string
    level: number
    topic: string
    questions: object[]
    grammars: string[]
    exerciseId: string
    createAt: number
}